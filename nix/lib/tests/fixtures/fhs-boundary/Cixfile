FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs

BUILDER build
  IMPORT ${pkgs.bash} ${pkgs.coreutils} ${pkgs.glibc}
  RUN printf 'must not replay through CIP-94\n' > result.txt

ITEM fhs-boundary
  COPY ${build}/result.txt /result.txt
