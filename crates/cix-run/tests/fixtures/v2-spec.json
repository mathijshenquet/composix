{
  "cixManifest": 0,
      "setup": [
        "bin/setup",
        "--admin-port",
        "$ADMIN_PORT"
      ],
      "exec": [
        "bin/web",
        "--admin-port",
        "$ADMIN_PORT"
      ],
      "env": {
        "ADMIN_PORT": {
          "default": "9000"
        }
      },
      "ports": {
        "http": {
          "value": 80,
          "protocol": "tcp"
        },
        "admin": {
          "env": "ADMIN_PORT",
          "protocol": "tcp"
        }
      },
      "dirs": {
        "run": [
          "/run/web"
        ]
      },
      "claims": ["jit"]
}
