FROM nixpkgs AS pkgs
SERVICE app
PATH /tools/first /tools/second
EXEC app $PORT
SETUP setup
ENV PORT = 9000 required
ENV TOKEN secret
PORT http = $PORT
STATE /var/lib/app
CACHEDIR /var/cache/app
LOGS /var/log/app
CONFIG /etc/app
RUNDIR /run/app
JIT
