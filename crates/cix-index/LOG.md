# Index track work log

- 2026-07-30T15:10:25Z — Started D45/index2 on `track/index2`. Read `AGENTS.md`, `.dev/specs/track-index2.md`, D44/D45 in `docs/design.md`, and `docs/compose-tree.md` §5. The devenv/direnv environment is active (`IN_NIX_SHELL=impure`). Current implementation is the retired per-ref `tags/*.json` plus `roots/<encoded-ref>` model; next is replacing it with immutable per-name tables, pointer CAS, migration, roots, history, and focused coverage.
- 2026-07-30T15:17:40Z — Implemented the D45 storage core: deterministic `table.json` store items per name, name-pointer JSON with locked atomic CAS/retry, current-table GC roots (table plus referenced paths), yanking, name deletion API, table-history walk, one-time `tags/` → `meta.legacy/` migration, and `cix index history`. Added focused real-Nix tests for round-trip/output golden, CAS conflict, reader atomicity, yank advisory semantics/root accounting, history, and migration. Green exact repro: `cargo test -p cix-index --no-fail-fast` (7 unit + 1 pull integration tests).
- 2026-07-30T15:22:16Z — Fixed a determinism leak caught by the tour: the temporary source directory name had become the Nix output basename, so equal table JSON produced different store paths. Tables now always enter the store as `table/`; focused coverage proves equal tables give equal `TablePointer`s. Reworked the tour's retired sidecar/root probes to show D45's name table and updated the compose-lock harness to read it. Green exact repro: `cargo test -p cix-index`; `cargo test -p cix --test tour -- --ignored generate_tour`; `cargo test -p cix --test tour` (tour drift and consecutive rendering both pass).
- 2026-07-30T15:23:15Z — Final gate green. Exact repro: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test -p cix-index && cargo test --workspace`; then `cargo test -p cix --test tour -- --ignored generate_tour && cargo test -p cix --test tour && git diff --exit-code -- docs/tour && git diff --check`. The focused index suite has 8 D45 unit tests plus the existing pull integration test; workspace tests and tour drift/determinism all pass, and regeneration leaves zero tour diff.

- 2026-07-28T00:00:00Z — Began Part 1 index implementation. Read `DESIGN.md` and `specs/track-index.md`; implementation will cover cix-common ref parsing/Nix helpers and cix-index local store, CLI, HTTP service, pull, tests, and demo.
- 2026-07-28T00:15:00Z — Added Docker-style ref parsing, subprocess wrappers, base64-url ref filenames, JSON sidecars, indirect GC-root registration, tag/untag/list, a tiny HTTP resolver/cache server, and pull with narHash verification. Initial compile identified only local type/move fixes, now corrected; next is full test/clippy plus real-Nix validation and demo.
- 2026-07-28T00:25:00Z — Workspace unit tests and clippy are green. Real Nix validation exposed Determinate Nix 2.34 `path-info --json` emitting a path-keyed object (not an array); adapted the parser and pinned JSON format 1 to avoid the CLI deprecation. Continuing with end-to-end verification.
- 2026-07-28T00:35:00Z — Ran a real two-state-dir publisher/client flow with `nix store add-path`, `tag`, `serve --with-store`, HTTP resolve, `pull --as`, and `ls`; it completed successfully. Added that flow as the required integration test and as a human-runnable `demo.sh`.
- 2026-07-28T00:45:00Z — Tightened cache/trust behavior: `--sign-key` now signs before copying into the binary cache, live servers cache newly added matching tags before responding, and pulls pass index `trustedKeys` to Nix while retaining Nix’s default signature requirement. narHash is independently rechecked after every fetch. Root URLs with optional path prefixes remain representationally ambiguous with slash-containing names under D12’s Docker disambiguation; the implementation follows the explicit first-component rule (host/port only).
- 2026-07-28T00:50:00Z — Final summary: implemented all requested index commands, local indirect GC roots and sidecars, per-system output metadata, resolver/cache HTTP API, Nix copy/sign integration, pull refresh, unit tests, real-Nix localhost integration test, and demo. `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace`, and `demo.sh` pass. Signature enforcement is Nix’s default required-signature policy, augmented with entry `trustedKeys`; narHash verification is always performed after copy. Deviation/open question: optional root URL path prefixes cannot be uniquely recovered from slash-containing names under D12’s first-component Docker rule, so refs currently treat only the qualifying first component as `root_url`; needs a future syntax/normalization decision. The checked-in direnv environment was blocked, so verification used the available workspace toolchain and installed Nix.
- 2026-07-28T01:00:00Z — Began web track. Read D12, D17 v2, D18 v2, the Part 1 HTTP surface/org workflow, and `specs/track-web.md`. The current server still uses configured-root `/v1` routes, so this pass will replace that surface with bare-tag serving and negotiated HTML/JSON, without changing the run crate or cix CLI wrapper.
- 2026-07-28T01:15:00Z — Replaced `serve <root_url>` with bare-tag serving, rejected public qualified `tag` targets, deleted `/v1` routing, and changed pull to negotiate the entry URL directly. Added escaped, minimal HTML pages and media-type/query negotiation with `Vary: Accept`; request Host/X-Forwarded-Proto now define self references and own cache advertisement. `--with-store` now rebuilds its derived cache from bare tags only, including after tag changes, so mirror closures are not retained. Focused integration coverage passes for the two-machine pull story, conneg, 404s, Host snippets, qualified-tag errors, and mirror hiding. Next: run the workspace gate and demo, then regenerate the tour only if its drift test requires it.
- 2026-07-28T01:25:00Z — Final summary: completed the D17/D18 v2 web refactor in `cix-index`, including bare-only serving/cache population, negotiated root/name/permalink representations, Host-derived snippets/substituter URLs, and direct negotiated pull resolution. Updated the integration test and demo. `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, `cargo test --workspace` (including tour drift), and `crates/cix-index/demo.sh` all pass. Deviations: none. Open questions: none.

- 2026-08-04T15:35:39Z — Started and implemented `track/inspectremote` in the active devenv (`IN_NIX_SHELL=impure`). Qualified `cix inspect` targets are now explicitly selected as the artifact world using the existing `cix_common::Ref::parse` grammar shared with `pull`; local unqualified behavior is unchanged. The shared remote resolver sends `Accept: application/vnd.cix+json;version=1`, rejects non-cix media types, and names the attempted URL in distinct connection, 404, and response-type failures. Extended the existing serve/pull fixture with served-ref inspection, all three error paths, and an assertion on the negotiation header. Focused synchronous receipts: `cargo test -p cix --bin cix`; `cargo test -p cix-index --test pull -- --nocapture`; `git diff --check` (all pass). Documented qualified inspect alongside pull and regraded the affected `docs/docker.md` image-inspect row. Pending tour touchpoint: do not edit `docs/tour` on this track; the orchestrator owns the in-flight demo weave.

- 2026-08-04T15:43:53Z — Standard agent tier receipts passed synchronously: `cargo fmt --all --check`; `cargo run -p cix -- fmt --check examples` (only existing LINK-deprecation warnings); `cargo clippy --workspace --all-targets -- -D warnings`; and `cargo test --workspace` (including committed-tour drift and determinism). The first workspace attempt collided with concurrent tour worktrees on the shared `cix-run-decoy-x.service`; after their read-only observed jobs cleared, the complete retry exited 0. Did not run the writing `generate_tour` target or any VM scenario: the request fences `docs/tour` to the orchestrator’s in-flight track, and this index/CLI protocol change has no focused VM scenario. Read-only CIP/corpus impact grep found no matching `corpus/migrate/*/GAPS.md` cases. Final next step: commit the reviewed scoped diff.
