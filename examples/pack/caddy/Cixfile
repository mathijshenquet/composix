FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs
FROM . AS src

SERVICE caddy
  COPY ${src}/index.html /srv/www/index.html
  START ${pkgs.caddy}/bin/caddy file-server --root /srv/www --listen :80
  PORT http = 80
