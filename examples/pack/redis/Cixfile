FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs
FROM . AS src

SERVICE redis
  COPY ${src}/redis.conf /etc/redis/redis.conf
  COPY ${pkgs.redis}/bin/redis-server /bin/redis-server
  ENV LANG=C
  ENV LC_ALL=C
  START redis-server /etc/redis/redis.conf
  PORT redis = 6379
  RUNDIR /run/redis
