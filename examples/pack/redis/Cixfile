FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs
FROM . AS src

BUILDER build
COPY ${src}/redis.conf etc/redis/redis.conf

SERVICE redis
PATH ${pkgs.redis}/bin
COPY ${build}/etc/redis/redis.conf etc/redis/redis.conf
EXEC redis-server /etc/redis/redis.conf
PORT redis = 6379
RUNDIR /run/redis
