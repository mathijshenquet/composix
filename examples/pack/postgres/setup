set -eu
. /opt/postgres/runtime-env.sh

if [ ! -s "$data_dir/PG_VERSION" ]; then
  rm -rf "$init_dir"
  cp -R --no-preserve=mode,ownership /opt/postgres/template "$init_dir"
  mv "$init_dir" "$data_dir"
fi
chmod 0700 "$data_dir"
