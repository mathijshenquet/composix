# Examples 2 work log

- 2026-07-29T00:00:00Z — Started `track-examples2`. Scope is limited to the PostgreSQL Cixfile polish, three new Cixfile-only examples, the Cixfile directive documentation, and the VM dogfood check. Next: convert PostgreSQL's generated scripts to checked-in sibling assets and verify both build paths.
- 2026-07-29T00:10:00Z — Converted PostgreSQL to `COPY`ed `runtime-env.sh`, `setup`, and `start` siblings with item-local package links and `dirname "$0"` discovery. `COPY` inputs are mode 0644, so the service invokes the scripts through a linked `bin/sh`; this preserves verbatim sources without Cixfile heredocs. `devenv shell -- cargo run -p cix -- build examples/postgres`, `nix-build examples/postgres --no-out-link`, and the sudo demo using the freshly built local cix binary passed; its transient unit stopped cleanly. Added the matching directive-reference subsection. Next: add Redis.
- 2026-07-29T01:05:00Z — Added Cixfile-only Redis, Caddy, and Node examples, each with sibling assets and a cleanup-trapped sudo demo. Redis serves TCP 6379 plus `/run/redis/redis.sock`; the demo uses sudo for the socket client because DynamicUser owns the runtime directory. Caddy served the copied static page on port 80 with `AmbientCapabilities=cap_net_bind_service`. The Node no-JIT control had `MemoryDenyWriteExecute=yes` and V8 aborted in `v8::base::OS::SetPermissions`; the JIT item omitted that restriction and served its HTTP response. Next: extend VM dogfood with the same scenarios.
- 2026-07-29T01:10:00Z — Extended VM dogfood with equivalent Redis, Caddy, and Node items sourced from the Cixfile sibling assets, retaining Cixfile-only example directories. The VM runs and stops each service in sequence, checks both Redis transports, proves Caddy's port-80 capability and response, and checks Node's JIT unit effect plus response. This adds three short start/probe/stop cycles without nested in-VM Cixfile builds. Next: run the VM check and final repeated demos/gates.
- 2026-07-29T01:20:00Z — `nix build .#checks.x86_64-linux.vm-dogfood -L` passed. PostgreSQL, Redis, Caddy, and Node sudo demos each passed twice with the freshly built cix binary; the no-JIT Node control consistently aborted at V8 permission setup and all transient services were stopped. `cargo fmt --all --check` and warning-denied workspace clippy passed. Finding: plain `cargo test --workspace` is currently not green on this host solely because the pre-existing `crates/cix/tests/tour.rs` tests call `render_tour()` repeatedly against the shared user systemd manager; retained `cix-run` slice/listener state changes the generated `cix ps` table width and listener-stop transcript. No crate or out-of-scope tour-doc change was made for this examples-only track. Both managers were left with no `cix-*` units after cleanup.
