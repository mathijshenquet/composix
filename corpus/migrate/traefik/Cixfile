FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs

SERVICE traefik
  LINK ${pkgs.traefik}/bin/traefik /bin/traefik
  START traefik --ping=true --entryPoints.web.address=:8081
  PORT http = 8081
  PORT monitor = 8080
