FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs
FROM . AS src

SERVICE redis
  COPY ${src}/redis.conf /etc/redis/redis.conf
  START ${pkgs.redis}/bin/redis-server /etc/redis/redis.conf
  PORT redis = 36379
  RUNDIR /run/redis
  READINESS tcp :36379 IN 20s
