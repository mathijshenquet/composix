FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs

SERVICE caddy
  STATE /var/lib/caddy
  ENV XDG_CONFIG_HOME = /var/lib/caddy/config
  ENV XDG_DATA_HOME = /var/lib/caddy/data
  EXEC ${pkgs.caddy}/bin/caddy respond --listen :8080 Caddy
  PORT http = 8080
