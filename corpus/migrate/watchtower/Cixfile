FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs
FROM . AS src

BUILDER build
  PATH ${pkgs.bash}/bin ${pkgs.coreutils}/bin ${pkgs.go}/bin ${pkgs.gcc}/bin
  COPY ${src}/context/ .
  FETCH SSL_CERT_FILE=${pkgs.cacert}/etc/ssl/certs/ca-bundle.crt GOMODCACHE=$PWD/.gomodcache ${pkgs.go}/bin/go mod download
  RUN GOMODCACHE=$PWD/.gomodcache go build -o watchtower .

SERVICE watchtower
  COPY ${build}/watchtower bin/watchtower
  PATH bin
  EXEC watchtower
  PORT http = 8080
