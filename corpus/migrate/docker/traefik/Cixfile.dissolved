FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs

SERVICE traefik
  IMPORT ${pkgs.cacert} ${pkgs.traefik} ${pkgs.tzdata}
  START traefik --ping=true
  PORT http = 80
  PORT ping = 8080
  CLAIM egress
