FROM github:NixOS/nixpkgs/nixos-unstable AS pkgs
FROM . AS src

APP cleanup
  IMPORT ${pkgs.bash} ${pkgs.postgresql}
  COPY ${src}/cleanup.sh /opt/mastodon/cleanup.sh
  START bash /opt/mastodon/cleanup.sh psql
  SECRET db-password
