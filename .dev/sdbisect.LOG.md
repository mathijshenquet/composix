# systemd 257..261 upstream investigation journal

- 2026-07-30 20:23 UTC — After making both kernel cells explicit expected controls, final verification passed again: nix build .#sdbisect-revert-vm --no-link -L. Result: /nix/store/x08c5z9i4wca708zv7hb0fls9jcs97ay-vm-test-run-sdbisect-revert. Persistent transcript: nix-store --read-log /nix/store/7b61qvfkmj6sdbwxmpbv5lh8p1yg1ss9-vm-test-run-sdbisect-revert.drv. The five-node test finished in 71.48 seconds and records kernel axis result: 6.17=uid-map-eperm, 6.18=uid-map-eperm. git diff --check passed. Next: commit kernel-axis harness and documentation.

- 2026-07-30 20:18 UTC — Final cleaned-harness verification passed: nix build .#sdbisect-revert-vm --no-link -L. Result: /nix/store/v05qzrrakwadbh43by59szxh9622a28r-vm-test-run-sdbisect-revert. Persistent transcript: nix-store --read-log /nix/store/00n2pll3fxgc1hpjzw8v6arrfqcl6sr9-vm-test-run-sdbisect-revert.drv. The rebuilt test script type-checks and lints, then finishes in 72.10 seconds with kernel axis result: 6.17=uid-map-eperm, 6.18=uid-map-eperm. Added a journal wait after the failing systemctl start so the UID-map assertion cannot race journald. Next: diff check and commit.

- 2026-07-30 20:13 UTC — Decisive kernel-axis comparison complete. The initial 19:59 entry's 20:xx timestamp was a placeholder; 19:59 UTC is the correct time. The same stock-systemd-261 service on both Linux 6.17.13 and 6.18.40 reports: kernel axis result: 6.17=uid-map-eperm, 6.18=uid-map-eperm. Each cell checks its uname -r prefix and systemd 261; the exact DynamicUser=yes + PrivatePIDs=yes + StateDirectory=sdbisect service exits 1 and logs Failed to write UID map: Operation not permitted plus status=226/NAMESPACE. Exact successful command: nix build .#sdbisect-revert-vm --no-link -L. Output: /nix/store/hknig8izqwc0q5q1f54svbfag8x7jgsr-vm-test-run-sdbisect-revert. Log: nix-store --read-log /nix/store/5ikhnqxq6qj6dac50k8f38nfiiyg9m0l-vm-test-run-sdbisect-revert.drv. Current nixpkgs exposes linuxPackages_6_17 but evaluating it throws its EOL error, so added input github:NixOS/nixpkgs/ef6c19e8baf55f671169995f0fa532511062a99a; verification command nix eval --raw github:NixOS/nixpkgs/ef6c19e8baf55f671169995f0fa532511062a99a#linuxPackages_6_17.kernel.version prints 6.17.13. The first run stopped only because the harness over-escaped its uname regex before it tried the service; it was corrected to compare the prefix directly, then this successful run completed. Conclusion: not a kernel-only 6.17→6.18 change in the VM. Do not search kernel git/lore or target a kernel issue from this evidence; remaining host-vs-VM configuration differences are documented in .dev/upstream-systemd-226-namespace.md. Next: final evaluation/diff checks and commit.

- 2026-07-30 20:xx UTC — Resumed kernel-axis work as requested. Fast-forward merged `main` (`15f737b..c43ae9b`), then reread the journal tail and the upstream draft. The existing three-node harness proves the exact multi-line `/proc/<pid>/uid_map` write returns EPERM under Linux 6.18.40 for both systemd 257.6 and 261. Next: discover the closest Linux 6.17 kernel package exposed by this pinned nixpkgs, add same-systemd VM cells that differ only in `boot.kernelPackages`, and run their direct comparison.

- 2026-07-30 19:58 UTC — The diagnostic three-node run completed successfully with the 257 failure intentionally asserted: `nix build .#sdbisect-revert-vm --no-link -L`; final derivation `/nix/store/vb5f5ih90s3hlwvnf6p01k98avjb8v8q-vm-test-run-sdbisect-revert.drv` (full evidence: `nix-store --read-log /nix/store/vb5f5ih90s3hlwvnf6p01k98avjb8v8q-vm-test-run-sdbisect-revert.drv`). All three nodes used Linux 6.18.40 in the same VM runner. Stock/reverted systemd 261 fail as before; v257 reports systemd 257, PID 1 resolves to the pinned-257.6 root manager copy, and it too reports `Failed to write UID map: Operation not permitted` followed by 226/NAMESPACE. This falsifies the expected 257-pass cell and rules out a 257→261 version-only regression on this harness. Updated `.dev/upstream-systemd-226-namespace.md`: it now names the exact `openat`/`write` EPERM and states that no upstream systemd regression claim/file should be made from the present evidence. Next: final diff/build checks and commit.

- 2026-07-30 19:55 UTC — The three-node build reached VM execution. Stock and reverted 261 cells behaved as asserted; v257 booted, reported `systemd 257`, and `/proc/1/exe` resolved to the copied pinned-257.6 compatibility output, but `systemctl start sdbisect.service` unexpectedly exited 1. No regression conclusion can be drawn from that run. Temporarily made that outcome expected and enabled v257 manager debug logging so the next otherwise-passing test prints its complete unit journal. Re-run `nix build .#sdbisect-revert-vm --no-link -L` and inspect the v257 failure before altering the compatibility layer or the conclusion.

- 2026-07-30 19:52 UTC — The writable copied 257 package completed, and modern NixOS next requested another post-257 unit, `systemd-ask-password@.service`. Replaced the single varlink placeholder with a loop that creates the same inert `[Unit]` placeholder for every direct current-systemd example unit absent from v257. This only satisfies NixOS's unit-linker input contract; no v257 executable/library/code is replaced. Retry `nix build .#sdbisect-revert-vm --no-link -L`.

- 2026-07-30 19:50 UTC — Resumed after fast-forward merging `main` (`41c85e8..0d846d5`) and rereading this log/draft. Stock-261 capture passed via `nix build .#sdbisect-revert-vm --no-link -L` with `systemd.settings.Manager.LogLevel = "debug"`. The journal confirms the namespace worker forks, then fails `Failed to write UID map: Operation not permitted`, before setgroups/GID map/mount_setattr. A PID-1 trace run (`strace -ff -p 1 -e trace=%process,mount_setattr,openat,write,unshare,setns`) gave the exact record in its Nix log: `openat(AT_FDCWD, "/proc/2/uid_map", O_WRONLY|O_NOCTTY|O_CLOEXEC) = 5`; `write(5, "65534 61222 1\\n0 0 1\\n", 20) = -1 EPERM (Operation not permitted)`. This is the temporary user namespace in the PID-namespace child; earlier capability-probe map opens and later mount_setattr calls are not the failure. The harness asserts the uid-map-adjacent EPERM and keeps the full journal/trace under `/tmp` for the test run. Exact prior trace derivation: `/nix/store/nflyhgg0hhd1viil770ylqnazi4il2as-vm-test-run-sdbisect-revert.drv` (inspect with `nix-store --read-log …`).

- 2026-07-30 19:50 UTC — Added Nixpkgs pin `0002d4fba62a97fe1260dc41f00deaac9a53f63d`, verified by `nix eval --raw github:NixOS/nixpkgs/0002d4fba62a97fe1260dc41f00deaac9a53f63d#systemd.version` → `257.6`, and locked it with `nix flake lock --update-input nixpkgs-systemd257` (deprecated-alias warning only). Added a third root-system v257 VM and manager/version/start assertions. Current NixOS modules require post-257 package API flags and a `systemd-udevd-varlink.socket` unit absent from 257, while its initrd expects a newer breakpoint unit. The harness therefore uses current systemd only in initrd, and a copied, otherwise unmodified 257.6 root-system package with feature-attribute compatibility values, one inert varlink-unit placeholder, and rewrites of textual self-path references. This still executes the pinned 257 manager binary; the test asserts PID 1 resolves in that copy. Current retry command is `nix build .#sdbisect-revert-vm --no-link -L`; it has reached the copied-package build but needs `chmod -R u+w "$out"` before the text rewrite because copied config directories are read-only. Next: rerun and obtain the three-cell verdict, then update the upstream draft and commit.

- 2026-07-30 19:37 UTC — Resumed `track/sdbisect` after fast-forward merging `main` (`41c85e8..0d846d5`; clean worktree). Re-read this journal and the upstream issue draft. The first priority is now an instrumented stock-261 VM run: enable PID 1 debug logging and preserve/print the complete failed-start journal; if it does not identify the low-level `EPERM`, attach `strace -f` to PID 1 around the start with process/mount/user-namespace/open/write tracing. For the same-harness historical cell, queried Nixpkgs history and selected immutable revision `0002d4fba62a97fe1260dc41f00deaac9a53f63d` (2025-05-30, `systemd: 257.5 -> 257.6`); `nix eval github:NixOS/nixpkgs/nixos-24.11#systemd.version` instead reports 256.10, so it is unsuitable. Next: add the debug harness, run it, then add the pinned 257 node and run the three-node check.

- 2026-07-30 19:37 UTC — First debug-harness evaluation command, `nix build .#sdbisect-revert-vm --no-link -L`, stopped before a VM build because current NixOS rejects the obsolete `systemd.extraConfig`: it says it has no effect and requires `systemd.settings.Manager`. Replaced that setting with `systemd.settings.Manager.LogLevel = "debug"`; retry the same command.

- 2026-07-30 19:37 UTC — The corrected debug build completed successfully (including the expected stock and reverted failures), and the test collected the complete stock boot journal in `/tmp/sdbisect-stock-journal.txt` immediately after the failed start. Printing that complete journal made the Nix build log exceed the command-output limit, so its relevant unit-scoped debug lines were not recoverable from the captured terminal output. Kept the full-journal collection and changed the emitted test evidence to the unit journal filtered for the user-namespace/id-map path; re-run with `nix build --rebuild .#sdbisect-revert-vm --no-link -L` before deciding whether ptrace is needed.

- 2026-07-30 19:40 UTC — The revised debug harness passed: `nix build .#sdbisect-revert-vm --no-link -L` (the test result is `/nix/store/hrnkkiy04aygs8lrwc30ffjykcl9nrpa-vm-test-run-sdbisect-revert`; exact output recovered with `nix-store --read-log $(nix path-info --derivation .#sdbisect-revert-vm) | tail -n 80`). The stock-261 unit journal shows the namespace worker forked `(sd-mkuserns)` as PID 2, then `Failed to write UID map: Operation not permitted`, followed by the generic allocate-user-namespace/NAMESPACE wrapper. It therefore localizes the operation to writing `/proc/<sd-mkuserns>/uid_map`, before `setgroups`, `gid_map`, or `mount_setattr`; debug does not show the syscall record itself. Added `strace -ff -p 1 -e trace=%process,mount_setattr,openat,write,unshare,setns` around a fresh failed start, with its per-PID output in `/tmp/sdbisect-strace.*`, and will run it next.

- 2026-07-30 19:43 UTC — The strace harness passed: `nix build .#sdbisect-revert-vm --no-link -L`; the persistent build log is `nix-store --read-log /nix/store/nflyhgg0hhd1viil770ylqnazi4il2as-vm-test-run-sdbisect-revert.drv`. PID 1 strace followed the service worker and records the exact failing syscall: `openat(AT_FDCWD, "/proc/2/uid_map", O_WRONLY|O_NOCTTY|O_CLOEXEC) = 5`, then `write(5, "65534 61222 1\\n0 0 1\\n", 20) = -1 EPERM (Operation not permitted)`. This is the temporary id-mapped-mount user namespace in the PID-namespace child (`/proc/2` there), and it is before any setgroups/GID-map write or the successful later `mount_setattr` calls. The run also records unrelated earlier capability-probe opens of `/proc/730/{uid_map,setgroups,gid_map}` and successful mount_setattr calls; neither is the failure. Added an assertion for the uid-map-adjacent `EPERM` trace line. Confirmed the selected immutable Nixpkgs pin directly with `nix eval --raw github:NixOS/nixpkgs/0002d4fba62a97fe1260dc41f00deaac9a53f63d#systemd.version`, which prints `257.6`. Next: lock the pin, add this 257 package as the third same-harness VM node, and run the three-node test.

- 2026-07-30 19:43 UTC — Locked the 257.6 pin with `nix flake lock --update-input nixpkgs-systemd257` (Nix warns that the alias is deprecated, but it added exactly `0002d4f`, dated 2025-05-30, to `flake.lock`). The first three-node evaluation stopped before a VM build because current NixOS modules expect `systemd.package.withLogind`, an attribute not present in the old package. This is a module/package API mismatch, not a test result. Added a narrow derivation-attribute compatibility shim: interface version 2; logind true; FIDO2/importd/machined/sysupdate/TPM2 false. It changes only current-module feature gating and not the old systemd store path/binary under test. Retry `nix build .#sdbisect-revert-vm --no-link -L`.

- 2026-07-30 19:44 UTC — The retry again stopped at Nix evaluation, now because current `nixos/modules/system/boot/systemd.nix` also requires `systemd.package.withNspawn`. Audited every `cfg.package.with*` reference in that module and extended the same compatibility shim with the remaining booleans (bootloader, cryptsetup, EFI, nspawn, portabled, repart, timedated, localed, hostnamed, UTMP). These fields only determine which packaged system unit files the modern module links; they do not replace or patch the v257 manager. Retry the same build.

- 2026-07-30 19:45 UTC — With the full shim, the v257 root system evaluated and fetched the cached old manager, but its build stopped while assembling the initrd: modern NixOS requests `breakpoint-pre-udev.service`, which systemd 257 does not ship. `boot.initrd.systemd.package` had inherited the v257 override, so set it explicitly to current `pkgs.systemd`; the initrd manager is only used before switch-root, while the root-system PID 1 remains the asserted v257 store path. This removes a harness-version incompatibility without changing the systemd manager under test. Re-run the three-node build.

- 2026-07-30 19:47 UTC — The current-initrd retry passed initrd assembly but stopped when the modern root-system module unconditionally links `systemd-udevd-varlink.socket`, introduced after 257. The old manager package does not ship that unused socket unit. Added a lightweight compatibility output that copies the cached v257.6 package unchanged and adds only an inert `[Unit]` placeholder at that unit-file path; the systemd binary, libraries, and all test-relevant code remain exactly from the old pin. The wrapper preserves the old package's `kbd` and `util-linux` outputs plus the prior module feature attributes. Retry the three-node build.

- 2026-07-30 19:48 UTC — The compatibility-output build initially failed because `cp -a` preserved the old package's read-only `example/systemd/system` directory; adding the placeholder then returned `Permission denied`. Added `chmod u+w` for that copied directory before creating the one compatibility unit. Retry the same build.

- 2026-07-30 19:49 UTC — The permission fix let the placeholder build, but current NixOS's coredump setting derivation then rejected the copied v257 example configuration: it substitutes the configured systemd output path, while the copied text still contained the original v257 store path. The compatibility output now rewrites only textual occurrences of that original path to its own path before adding the placeholder. This preserves the old binary and makes generated unit/config references self-consistent. Retry the same build.

- 2026-07-30 18:45 UTC — Decisive same-host NixOS VM revert experiment added on `track/sdbisect` (not yet run): `nix/sdbisect-revert-vm.nix` starts the minimal `Type=oneshot`, `ExecStart=/bin/true`, `DynamicUser=yes`, `PrivatePIDs=yes`, `StateDirectory=sdbisect` unit on two nodes. The stock systemd 261 control must fail and show both `Failed to allocate user namespace` and `status=226/NAMESPACE`; the reverted node must start the exact same unit and verify that PID 1 is the patched systemd store path. `flake.nix` imports a Nixpkgs package set with an overlay scoped only to this check; the overlay applies `nix/patches/systemd-6431c34b8a84-revert.patch` through `systemd.overrideAttrs`. The patch is the reverse of `git -C /home/mathijs/tmp/systemd-sdbisect diff --binary 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a^`, generated from the v261 upstream clone. The old common `.git/info/exclude` rule has been removed so this append-only journal is tracked, per the durable-memory policy. Next: evaluate/build `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L` and record whether the source patch applies and the full A/B verdict.

- 2026-07-30 00:00 UTC — Started on `track/sdbisect`. Read the upstream issue draft and the 16:50–16:52 UTC evidence in `crates/cix-run/LOG.md`. Goal: establish a same-harness non-NixOS v257/v261 result using upstream mkosi, then bisect if reproducible. The journal is worktree-locally ignored through `.git/info/exclude`.

- 2026-07-30 00:01 UTC — Environment check: `mkosi` was absent from PATH but `nix run nixpkgs#mkosi -- --version` fetched and reported mkosi 26. `/dev/kvm` exists but this account is not in the `kvm` group; KVM permission will be checked explicitly before the VM run.

- 2026-07-30 00:05 UTC — Cloned `https://github.com/systemd/systemd.git` with `--filter=blob:none` to `/home/mathijs/tmp/systemd-sdbisect`, checked out `v261` (`de9dbc37ad`), and generated upstream mkosi keys using `nix shell nixpkgs#mkosi nixpkgs#qemu --command mkosi -f genkey`. `sudo -n` can read and write `/dev/kvm`. The first image-build attempt, `nix shell nixpkgs#mkosi nixpkgs#qemu --command mkosi -f build`, stopped immediately because packaged mkosi 26 cannot satisfy the source configuration's `MinimumVersion=commit:60ed8c…`: `Cannot check mkosi git version, not running mkosi from a git repository`. Next: use the upstream mkosi Git checkout required by the systemd v261 hacking instructions, still with nix-provided runtime dependencies.

- 2026-07-30 00:09 UTC — Cloned current upstream mkosi to `/home/mathijs/tmp/mkosi-sdbisect` and successfully ran it through nix's Python environment (`mkosi 27~devel`). A Fedora/rawhide v261 build attempt (`nix shell nixpkgs#mkosi nixpkgs#qemu --command /home/mathijs/tmp/mkosi-sdbisect/bin/mkosi -f --distribution fedora --release rawhide build`) did not reach package/image construction: mkosi's unprivileged setup could not find `/run/systemd/io.systemd.NamespaceResource`, then `prctl` returned `EPERM`. This is a host policy constraint, not a systemd-under-test result. Since `sudo -n` and root KVM access are available, retry the same upstream harness as root.

- 2026-07-30 00:11 UTC — Root retry reached mkosi initialization, but its tools-tree dependency uses Debian before the selected Fedora main image and this NixOS host has no `/usr/share/keyrings/debian-archive-keyring.gpg`; mkosi stopped before downloading/building. The clone remains source-clean; the root-created `pkg/debian` is explicitly ignored upstream. Retry with mkosi's repository signature check disabled for this disposable local image, retaining Fedora/rawhide as the main userspace.

- 2026-07-30 00:14 UTC — Disabling the tools tree (`--tools-tree=`) removes the host-Debian bootstrap; mkosi then selects Fedora/rawhide, as confirmed by `summary`, and reaches the upstream `pkg/fedora` sync checkout. It stops at `dnf not found`: mkosi needs the guest distribution package manager available on the NixOS host. Next: provide `dnf5` from nixpkgs in the same root nix shell and retry.

- 2026-07-30 00:20 UTC — A scratch wrapper maps mkosi's required `dnf` name to nixpkgs `dnf5`. With `dnf5` and `rpm` added to the root nix shell, the v261 Fedora/rawhide build completed the source RPM and 3,306-step systemd RPM build, then stopped while constructing the `minimal-base` dependency image because `createrepo_c` was absent from the NixOS host. This is another mkosi host-tool prerequisite; add nixpkgs `createrepo_c` and reuse the completed build/cache on retry.

- 2026-07-30 00:24 UTC — With nixpkgs `createrepo_c` added, mkosi rebuilt/installed the source RPM and began its Fedora/rawhide minimal and initrd images. The v261 RPM compilation itself completed, but final initrd assembly failed before a guest could boot: `systemd` and `systemd-udev` `%sysusers` scriptlets each reported `usermod: cannot lock /etc/passwd; try again later`, then `dnf5 … --installroot=/buildroot … install … systemd … udev` exited 1. Thus there is no non-NixOS A/B result, no bisect, and no guest-level confirmation. The exact root command was `sudo -n /nix/var/nix/profiles/default/bin/nix shell nixpkgs#mkosi nixpkgs#qemu nixpkgs#dnf5 nixpkgs#rpm nixpkgs#createrepo_c -c sh -c 'PATH=/home/mathijs/tmp/sdbisect-bin:$PATH; exec /home/mathijs/tmp/mkosi-sdbisect/bin/mkosi -f --tools-tree= --repository-key-check=no --distribution fedora --release rawhide build'`. Per the requested fallback, proceed with a narrow v257..v261 source comparison.

- 2026-07-30 00:31 UTC — Targeted source audit completed in the same upstream clone. Exact review commands included `git log --format='%H %cs %s' --reverse v257..v261 -- src/core/exec-invoke.c`, `git log --format='%H %cs %s' --reverse v257..v261 -- src/core/namespace.c src/basic/namespace-util.c src/shared/mount-util.c`, `git show 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a -- src/basic/namespace-util.c src/core/namespace.c src/shared/mount-util.c`, and `git show v257:src/basic/namespace-util.c` / `git show v261:src/basic/namespace-util.c`. No behavior result means no causal commit can be proven. Strongest candidate: 6431c34b8a84 (2025-03-17), which adds a requested `/proc/<child>/setgroups = deny` write to `userns_acquire()` and makes the id-mapped persistent-directory caller pass `setgroups_deny=true`; its message explicitly says all existing users (including id-mapped mounts) can enable it without harm. The observed error is exactly the wrapper around this helper. The DelegateNamespaces= work (8234cd99, 38748596) changes sequencing but preserves non-delegated PID-before-mount setup for the root system service default; 698ac172 only reparents the PID namespace child. NEWS has no announced incompatibility. If a future same-harness A/B confirms the symptom, assess it as an unintended bug candidate, not intended behavior; today this remains conditional.

- 2026-07-30 18:33 UTC — Timestamp correction: the preceding journal entries were accidentally stamped `00:00`–`00:31 UTC`; this investigation actually ran during 18:18–18:32 UTC (the built RPM release records 18:27 UTC). Their order, commands, outputs, and conclusions are accurate; precise per-command wall-clock start times were not retained. Final checks: `git diff --check` passed; `.dev/sdbisect.LOG.md` is ignored through the worktree's common `.git/info/exclude`; documentation commits are `e2489e9` and `c5423e2`; the composix worktree is clean.

- 2026-07-30 18:45 UTC — Ran the same-host NixOS A/B check with `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L`. The prepared full reverse of upstream `6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a` did not apply to the Nixpkgs systemd 261 source: the production `src/core/namespace.c` hunk applied, but three subsequent API/test-context hunks rejected. Nixpkgs contains later callers that deliberately pass both values, so the patch was narrowed to the candidate's production-relevant `StateDirectory=` call only: `userns_acquire(uid_map, gid_map, /* setgroups_deny= */ true)` → `false`.

- 2026-07-30 18:45 UTC — The focused A/B built the patched systemd 261 and booted both VMs. **Stock VM:** `systemctl --version` reported `systemd 261`; `systemctl start sdbisect.service` failed; its journal contained `Failed to allocate user namespace` and `status=226/NAMESPACE`. **Patched VM:** `/proc/1/exe` resolved under `/nix/store/adw22k0xhdfxiw40bx3q1vvxi2csmnwc-systemd-261`; it too failed `systemctl start sdbisect.service` with `Failed to allocate user namespace` and `status=226/NAMESPACE`. The test's then-positive patched assertion therefore failed. This A/B does **not confirm 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a as causal**.

- 2026-07-30 18:45 UTC — Updated the diagnostic assertion to encode the observed negative result for both VMs, retaining the stock/patch version and PID 1 store-path checks. Re-run command: `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L` (outcome recorded immediately after it completes).

- 2026-07-30 18:50 UTC — Re-run completed successfully (exit 0): `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L`. The VM test asserted systemd 261 on both nodes and that the patched node's PID 1 resolved under `/nix/store/z5s9mggpdimpq59ww73bypx4qhi9dsl0-systemd-261`. **Stock:** `systemctl start sdbisect.service` failed and its journal matched `Failed to allocate user namespace` and `status=226/NAMESPACE`. **Patched:** the same start command failed and the same two journal assertions matched. This confirms the negative A/B result: reversing the candidate's `StateDirectory=` caller change does not restore service startup.

- 2026-07-30 18:55 UTC — Final clean-patch verification completed successfully (exit 0): `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L`. The patch contains only the `StateDirectory=` caller change (no whitespace-only hunk context); Nix applied it to `src/core/namespace.c` and the two-node VM check again passed all version, patched-PID-1, expected-start-failure, and journal assertions. Both stock and patched systemd 261 therefore still fail with `Failed to allocate user namespace` and `status=226/NAMESPACE`.
