# systemd 257..261 upstream investigation journal

- 2026-07-30 18:45 UTC — Decisive same-host NixOS VM revert experiment added on `track/sdbisect` (not yet run): `nix/sdbisect-revert-vm.nix` starts the minimal `Type=oneshot`, `ExecStart=/bin/true`, `DynamicUser=yes`, `PrivatePIDs=yes`, `StateDirectory=sdbisect` unit on two nodes. The stock systemd 261 control must fail and show both `Failed to allocate user namespace` and `status=226/NAMESPACE`; the reverted node must start the exact same unit and verify that PID 1 is the patched systemd store path. `flake.nix` imports a Nixpkgs package set with an overlay scoped only to this check; the overlay applies `nix/patches/systemd-6431c34b8a84-revert.patch` through `systemd.overrideAttrs`. The patch is the reverse of `git -C /home/mathijs/tmp/systemd-sdbisect diff --binary 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a^`, generated from the v261 upstream clone. The old common `.git/info/exclude` rule has been removed so this append-only journal is tracked, per the durable-memory policy. Next: evaluate/build `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L` and record whether the source patch applies and the full A/B verdict.

- 2026-07-30 00:00 UTC — Started on `track/sdbisect`. Read the upstream issue draft and the 16:50–16:52 UTC evidence in `crates/cix-run/LOG.md`. Goal: establish a same-harness non-NixOS v257/v261 result using upstream mkosi, then bisect if reproducible. The journal is worktree-locally ignored through `.git/info/exclude`.

- 2026-07-30 00:01 UTC — Environment check: `mkosi` was absent from PATH but `nix run nixpkgs#mkosi -- --version` fetched and reported mkosi 26. `/dev/kvm` exists but this account is not in the `kvm` group; KVM permission will be checked explicitly before the VM run.

- 2026-07-30 00:05 UTC — Cloned `https://github.com/systemd/systemd.git` with `--filter=blob:none` to `/home/mathijs/tmp/systemd-sdbisect`, checked out `v261` (`de9dbc37ad`), and generated upstream mkosi keys using `nix shell nixpkgs#mkosi nixpkgs#qemu --command mkosi -f genkey`. `sudo -n` can read and write `/dev/kvm`. The first image-build attempt, `nix shell nixpkgs#mkosi nixpkgs#qemu --command mkosi -f build`, stopped immediately because packaged mkosi 26 cannot satisfy the source configuration's `MinimumVersion=commit:60ed8c…`: `Cannot check mkosi git version, not running mkosi from a git repository`. Next: use the upstream mkosi Git checkout required by the systemd v261 hacking instructions, still with nix-provided runtime dependencies.

- 2026-07-30 00:09 UTC — Cloned current upstream mkosi to `/home/mathijs/tmp/mkosi-sdbisect` and successfully ran it through nix's Python environment (`mkosi 27~devel`). A Fedora/rawhide v261 build attempt (`nix shell nixpkgs#mkosi nixpkgs#qemu --command /home/mathijs/tmp/mkosi-sdbisect/bin/mkosi -f --distribution fedora --release rawhide build`) did not reach package/image construction: mkosi's unprivileged setup could not find `/run/systemd/io.systemd.NamespaceResource`, then `prctl` returned `EPERM`. This is a host policy constraint, not a systemd-under-test result. Since `sudo -n` and root KVM access are available, retry the same upstream harness as root.

- 2026-07-30 00:11 UTC — Root retry reached mkosi initialization, but its tools-tree dependency uses Debian before the selected Fedora main image and this NixOS host has no `/usr/share/keyrings/debian-archive-keyring.gpg`; mkosi stopped before downloading/building. The clone remains source-clean; the root-created `pkg/debian` is explicitly ignored upstream. Retry with mkosi's repository signature check disabled for this disposable local image, retaining Fedora/rawhide as the main userspace.

- 2026-07-30 00:14 UTC — Disabling the tools tree (`--tools-tree=`) removes the host-Debian bootstrap; mkosi then selects Fedora/rawhide, as confirmed by `summary`, and reaches the upstream `pkg/fedora` sync checkout. It stops at `dnf not found`: mkosi needs the guest distribution package manager available on the NixOS host. Next: provide `dnf5` from nixpkgs in the same root nix shell and retry.

- 2026-07-30 00:20 UTC — A scratch wrapper maps mkosi's required `dnf` name to nixpkgs `dnf5`. With `dnf5` and `rpm` added to the root nix shell, the v261 Fedora/rawhide build completed the source RPM and 3,306-step systemd RPM build, then stopped while constructing the `minimal-base` dependency image because `createrepo_c` was absent from the NixOS host. This is another mkosi host-tool prerequisite; add nixpkgs `createrepo_c` and reuse the completed build/cache on retry.

- 2026-07-30 00:24 UTC — With nixpkgs `createrepo_c` added, mkosi rebuilt/installed the source RPM and began its Fedora/rawhide minimal and initrd images. The v261 RPM compilation itself completed, but final initrd assembly failed before a guest could boot: `systemd` and `systemd-udev` `%sysusers` scriptlets each reported `usermod: cannot lock /etc/passwd; try again later`, then `dnf5 … --installroot=/buildroot … install … systemd … udev` exited 1. Thus there is no non-NixOS A/B result, no bisect, and no guest-level confirmation. The exact root command was `sudo -n /nix/var/nix/profiles/default/bin/nix shell nixpkgs#mkosi nixpkgs#qemu nixpkgs#dnf5 nixpkgs#rpm nixpkgs#createrepo_c -c sh -c 'PATH=/home/mathijs/tmp/sdbisect-bin:$PATH; exec /home/mathijs/tmp/mkosi-sdbisect/bin/mkosi -f --tools-tree= --repository-key-check=no --distribution fedora --release rawhide build'`. Per the requested fallback, proceed with a narrow v257..v261 source comparison.

- 2026-07-30 00:31 UTC — Targeted source audit completed in the same upstream clone. Exact review commands included `git log --format='%H %cs %s' --reverse v257..v261 -- src/core/exec-invoke.c`, `git log --format='%H %cs %s' --reverse v257..v261 -- src/core/namespace.c src/basic/namespace-util.c src/shared/mount-util.c`, `git show 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a -- src/basic/namespace-util.c src/core/namespace.c src/shared/mount-util.c`, and `git show v257:src/basic/namespace-util.c` / `git show v261:src/basic/namespace-util.c`. No behavior result means no causal commit can be proven. Strongest candidate: 6431c34b8a84 (2025-03-17), which adds a requested `/proc/<child>/setgroups = deny` write to `userns_acquire()` and makes the id-mapped persistent-directory caller pass `setgroups_deny=true`; its message explicitly says all existing users (including id-mapped mounts) can enable it without harm. The observed error is exactly the wrapper around this helper. The DelegateNamespaces= work (8234cd99, 38748596) changes sequencing but preserves non-delegated PID-before-mount setup for the root system service default; 698ac172 only reparents the PID namespace child. NEWS has no announced incompatibility. If a future same-harness A/B confirms the symptom, assess it as an unintended bug candidate, not intended behavior; today this remains conditional.

- 2026-07-30 18:33 UTC — Timestamp correction: the preceding journal entries were accidentally stamped `00:00`–`00:31 UTC`; this investigation actually ran during 18:18–18:32 UTC (the built RPM release records 18:27 UTC). Their order, commands, outputs, and conclusions are accurate; precise per-command wall-clock start times were not retained. Final checks: `git diff --check` passed; `.dev/sdbisect.LOG.md` is ignored through the worktree's common `.git/info/exclude`; documentation commits are `e2489e9` and `c5423e2`; the composix worktree is clean.

- 2026-07-30 18:45 UTC — Ran the same-host NixOS A/B check with `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L`. The prepared full reverse of upstream `6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a` did not apply to the Nixpkgs systemd 261 source: the production `src/core/namespace.c` hunk applied, but three subsequent API/test-context hunks rejected. Nixpkgs contains later callers that deliberately pass both values, so the patch was narrowed to the candidate's production-relevant `StateDirectory=` call only: `userns_acquire(uid_map, gid_map, /* setgroups_deny= */ true)` → `false`.

- 2026-07-30 18:45 UTC — The focused A/B built the patched systemd 261 and booted both VMs. **Stock VM:** `systemctl --version` reported `systemd 261`; `systemctl start sdbisect.service` failed; its journal contained `Failed to allocate user namespace` and `status=226/NAMESPACE`. **Patched VM:** `/proc/1/exe` resolved under `/nix/store/adw22k0xhdfxiw40bx3q1vvxi2csmnwc-systemd-261`; it too failed `systemctl start sdbisect.service` with `Failed to allocate user namespace` and `status=226/NAMESPACE`. The test's then-positive patched assertion therefore failed. This A/B does **not confirm 6431c34b8a8487fb50c9cb850bd7d3bf81ad9e2a as causal**.

- 2026-07-30 18:45 UTC — Updated the diagnostic assertion to encode the observed negative result for both VMs, retaining the stock/patch version and PID 1 store-path checks. Re-run command: `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L` (outcome recorded immediately after it completes).

- 2026-07-30 18:50 UTC — Re-run completed successfully (exit 0): `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L`. The VM test asserted systemd 261 on both nodes and that the patched node's PID 1 resolved under `/nix/store/z5s9mggpdimpq59ww73bypx4qhi9dsl0-systemd-261`. **Stock:** `systemctl start sdbisect.service` failed and its journal matched `Failed to allocate user namespace` and `status=226/NAMESPACE`. **Patched:** the same start command failed and the same two journal assertions matched. This confirms the negative A/B result: reversing the candidate's `StateDirectory=` caller change does not restore service startup.

- 2026-07-30 18:55 UTC — Final clean-patch verification completed successfully (exit 0): `nix build .#checks.x86_64-linux.sdbisect-revert-vm --no-link -L`. The patch contains only the `StateDirectory=` caller change (no whitespace-only hunk context); Nix applied it to `src/core/namespace.c` and the two-node VM check again passed all version, patched-PID-1, expected-start-failure, and journal assertions. Both stock and patched systemd 261 therefore still fail with `Failed to allocate user namespace` and `status=226/NAMESPACE`.
