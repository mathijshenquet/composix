{
  "name": "runtrace-pnpm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=$CIX_OUTPUT/app.js"
  },
  "devDependencies": {
    "esbuild": "0.25.8"
  }
}
