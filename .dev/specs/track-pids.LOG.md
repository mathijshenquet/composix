# track/pids work log

- 2026-07-29 12:00 UTC — Read the repository directives, D36 (and the D13/D34 context), the PID track spec, current work logs, generator/runtime/debug/exec paths, golden fixtures, Docker ledger, tour harness, and VM gate. Baseline branch is `track/pids` with a clean worktree. The generator centralizes systemd properties in `crates/cix-run/src/unit.rs`; `cix run` and `cix debug` already retry user-manager failures through the D13 namespace-degraded path, while `cix exec` already detects and joins a target PID namespace. Next: add the property with an explicit start-probed fallback, update goldens/docs/tour, then run the required gates and live demonstrations.

- 2026-07-29 12:10 UTC — Implemented D36. Full system/user sandbox units now include `PrivatePIDs=yes`; `UserDegraded` omits it and the D13 retry explicitly names it. System-mode `cix run` now retries only after a failed start diagnosed as a namespace failure, dropping `PrivatePIDs` with an explicit D36 warning instead of version-sniffing; system `cix debug` has the same fallback. Updated all unit/composition goldens, added generator mode and live-property assertions, amended the Docker ledger and the D34 historical sentence, documented the PID-1 duty beside the hardening set, regenerated the rootless tour, and added VM assertions for the property plus `cix exec`'s PID view. Focused verification passed: `cargo fmt --all --check`, `cargo test -p cix-run`, `cargo test -p cix-compose`, `cargo test -p cix --test tour -- --ignored generate_tour`, and `git diff --check`. Next: root live nginx/exec and PostgreSQL checks, then the full workspace and VM gates.

- 2026-07-29 12:20 UTC — Live root checks used the freshly built `target/debug/cix`. This host's system manager is systemd 257, but it rejects namespace setup with `Failed to allocate user namespace: Operation not permitted`; the new start probe emitted the D36 warning and retried without `PrivatePIDs`. Nginx then served `hello from composix`; `systemctl show` reported `PrivatePIDs=no` and `cix exec --root` accurately reported PID among caller-shared namespaces, seeing host `/proc` (549 numeric entries, `/proc/1` = `systemd`). PostgreSQL took the identical explicit fallback, stayed active, and its own `psql` returned `1` for `SELECT 1`. This is platform-capability fallback evidence, not PID-1 application failure; it cannot satisfy the positive PID-namespace observation locally. Stopped all created system/user units and reset the one failed transient from tour regeneration; both managers now show no `cix-*` units. Next: the VM dogfood check provides the required supported-host PID proof.

- 2026-07-29 12:30 UTC — First `nix build .#checks.x86_64-linux.vm-dogfood -L` intentionally reached the new assertion and failed: although its systemd is 261, the VM inherited `user.max_user_namespaces=0`, so systemd emitted the same `Failed to allocate user namespace` diagnostic and D36 fallback, making `PrivatePIDs=no`. Added `boot.kernel.sysctl."user.max_user_namespaces" = 15000` to the disposable VM node so the required dogfood proof exercises the supported system-mode path. Re-running the VM gate now.

- 2026-07-29 12:35 UTC — The limit alone did not enable allocation: the second VM run again reached the `PrivatePIDs=no` assertion after `EPERM`. Added NixOS `security.unprivilegedUsernsClone = true` and a direct `unshare --user --map-root-user true` guest preflight before cix starts. This keeps the production fallback intact while making the dogfood VM's supported-path assumption explicit and observable. Re-running the VM gate.

- 2026-07-29 12:45 UTC — The direct non-root user-namespace preflight passed, but a DynamicUser + PrivatePIDs unit still failed allocating a user namespace. D34 requires system-mode exec root to be real root (no user namespace), so the generator now makes its system-mode default explicit with `PrivateUsers=no`, immediately after `DynamicUser=yes`; this avoids systemd taking the id-mapped/user-namespace route while retaining DynamicUser. Updated every generated-unit golden. Removed the temporary VM userns tuning/preflight so the final dogfood run proves the normal system-manager configuration. Re-running all affected checks.

- 2026-07-29 12:55 UTC — The explicit `PrivateUsers=no` did not alter the VM failure and was removed: systemd's id-mapped DynamicUser managed directories independently create a short-lived helper user namespace, while the service itself remains outside a user namespace per D34. The guest kernel has `kernel.apparmor_restrict_unprivileged_userns=1`; that restriction explains why a root `unshare` preflight passed but systemd's DynamicUser helper did not. Configured only the disposable dogfood VM with `kernel.apparmor_restrict_unprivileged_userns=0`, the existing DynamicUser runtime prerequisite, and retained the production D36 fallback. Next: re-run VM dogfood, then correct the exec assertion if namespace-local process numbering proves the shell is not PID 1.

- 2026-07-29 13:05 UTC — The VM kernel has no AppArmor (`kernel.apparmor_restrict_unprivileged_userns` is absent), so that attempted test-only sysctl was ignored and the service still fell back. The remaining relevant guest limits are `user.max_user_namespaces` and `user.max_pid_namespaces`; enabled both at 15000 in the disposable VM and added assertions plus a combined user/PID namespace `unshare` preflight. This is configuration for the test kernel's namespace resource limits, not a generator change. Next: re-run dogfood and inspect whether the supported systemd path starts.

- 2026-07-29 13:35 UTC — Isolated the supported-manager limitation with VM probes. On systemd 261, a plain `PrivatePIDs=yes` service and a `DynamicUser=yes` service can start, but `DynamicUser=yes + PrivatePIDs=yes + CacheDirectory=` fails at `Failed to allocate user namespace` while systemd prepares the id-mapped cache mount. This is a systemd/kernel composition rejection, so cix's start-probed D36 fallback is the correct loud behavior for directory-backed examples; it is not an application PID-1 failure. Removed all temporary VM sysctl/preflight diagnostics. The permanent VM gate now adds a directory-free cix `pid-probe` item: it asserted `PrivatePIDs=yes`, `cix exec --root` printed `private namespaces [mount, network, PID]`, and `/proc/1/comm` inside the joined view was `pid-probe`. Nginx and PostgreSQL still passed their health checks through the generated default/fallback path. Node (a pre-existing VM dogfood example) ran as PID 1 and served its JIT endpoint, but did not exit on SIGTERM; systemd waited 90 seconds and sent SIGKILL. Per the D36/spec boundary, this is recorded as PID-1 evidence rather than worked around. The VM test itself completed successfully (`run the VM test script`, 151.19s). Next: final workspace gate and commit.

- 2026-07-29 13:40 UTC — Final verification passed: `cargo build`; `cargo test` (including tour drift and determinism); `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; and `git diff --check`. `nix build .#checks.x86_64-linux.vm-dogfood -L` also passed, with the PID probe and all existing examples completing. Ready to commit `track/pids`.

- 2026-07-29 13:45 UTC — Final local cleanup stopped and reset the remaining root-owned `cix-run.slice` failed transients with `sudo systemctl stop cix-run.slice` and `sudo systemctl reset-failed 'cix-run-*'`; both system and user managers now list no `cix-*` units. Proceeding to commit.
