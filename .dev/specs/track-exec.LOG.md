# track/exec work log

- 2026-07-29 10:50 UTC — Started `.dev/specs/track-exec.md` on clean branch `track/exec`. Read `AGENTS.md`, the repository session log, and authoritative D34 plus its dependencies D13, D29c, and the D31 addendum. Contract: build `debug` first through the existing generator, then `exec` with direct namespace joining if practical; preserve the shared effective-PATH shell chain, loud user-mode degradation, and D34's deliberate absence of synthetic seccomp/cgroup controls for live exec. The repository direnv is active. Next: inventory CLI, generator/runtime boundaries, systemd parsing behavior, tour harness, and ledger before choosing the namespace implementation.

- 2026-07-29 10:53 UTC — Inventory complete. `cix-run` already exposes D29c's compiled unit (properties, environment, argv), and user-mode capability/mount fallbacks live in `runtime.rs`; debug can reuse both without a second sandbox compiler. Chose direct libc `setns(2)` over `nsenter`: open mount/pid/net/ipc/uts descriptors from `MainPID`, join them, fork after the pid join, drop groups/GID/UID in the child, then `execve` and wait in the parent. A live `cix-exec-property-probe` confirmed systemd 257 reports `MainPID`, numeric `UID`/`GID`, and `Environment=PLAIN=value "SPACED=a b" "QUOTED=a\"b"` exactly in the expected `systemctl show` surface; all five namespace handles are available under `/proc/<pid>/ns`. The probe was stopped by an EXIT trap. Next: implement shared shell resolution and debug, with unit/golden coverage before the live path.

- 2026-07-29 11:02 UTC — Core implementation and focused verification pass. Added `cix debug`/`cix exec`, shared effective-PATH `sh` → `/bin/sh` → explicit-command error resolution, quoted systemd `Environment=` parsing, exact/unique/ambiguous/missing live-target selection, and direct setns/fork/identity-drop exec. `cargo test -p cix-run --lib` passes 36 tests; warning-denied crate clippy and `cargo build -p cix` pass. Rootless debug exposed an existing missing `%t` expansion in the shared user `BindPaths` transport; adding `XDG_RUNTIME_DIR` expansion lets it reach the established D13 mount-namespace fallback. Live `debug --user -- /bin/sh -c ...` then ran as caller UID 1001 with the loud degraded warnings.

- 2026-07-29 11:02 UTC — Required system live paths exercised against `/nix/store/cx83…-nginx-cix`. Interactive `sudo cix debug <item>` selected `/bin/sh`, reported the DynamicUser, read `daemon off;` through `/etc/nginx/nginx.conf`, wrote `/var/cache/nginx/interactive-write` as the transient UID/GID, and proved `touch /etc/cix-interactive-denied` was blocked. A one-shot form repeated the projection/write/denial checks, and `exit 42` propagated as CLI status 42. Detached `cix run` produced `cix-run-nginx-18c6bdad7e1e86570.service`; `sudo cix exec nginx -- ...` saw nginx master/worker processes, ran as UID/GID 61406, and created `/var/cache/nginx/exec-write` owned 61406:61406. Exact-unit selection with `--root -- id` returned `uid=0(root)`. Cleanup traps/stops removed every tested unit. The nginx fixture declares cache+run rather than state, so the ownership transcript uses its actual writable cache role; this exercises the same systemd managed-directory identity mechanism without inventing an unused example state grant. Next: deterministic debug tour, ledger, full workspace/VM gate, and clean-system audit.

- 2026-07-29 11:10 UTC — Final gate passed on the finished source: `cargo fmt --all --check`, `cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, and `nix build .#checks.x86_64-linux.vm-dogfood`. The deterministic tour gained `11-debugging-service.md`; generation plus repeated drift/determinism tests pass. `docs/docker.md` now marks both exec and debug per D34, removes exec from the thin-verb gap, and records the pet-server-shell refusal. Live `exec --user` also passed against the rootless tour fixture as caller UID 1001 with the unit's recorded `CIX_APP`; systemd user units report `UID=[not set]`, correctly irrelevant on the D13 no-join path. The first VM attempt omitted the untracked new modules because Git-backed flakes exclude them; staging them made the actual source visible and the rerun passed. Final audit found no loaded cix units. It also exposed 400 historical generated listener unit files under `/run/user/1001/systemd/user`; after confirming none were loaded, removed only `cix-run-listenfds-*`, reloaded the manager, and changed the tour guard to call `stop_service` so a fresh complete gate leaves no runtime files. No spec-boundary deviations beyond nginx's cache-vs-requested-state transcript noted above. Next: commit all verified changes on `track/exec`.

- 2026-07-29 11:16 UTC — Began correction round 1 after independent re-verification. The nginx manifest records no PATH, so the prior resolver searched an empty path and made the 11:02 `cix exec nginx -- id` / `--root -- id` transcript impossible as written. That entry also incorrectly described nginx process visibility as proof of a joined private PID namespace: this port-declaring unit shares the host PID and network namespaces, while systemd gives it distinct mount/IPC/UTS namespaces. The correction will preserve history, make effective command/shell lookup append `/usr/bin:/bin` after any recorded/generated PATH, test the empty-environment case, and align runtime/docs wording around joining only namespaces that differ from the caller's current namespace. Next: reproduce in focused tests and inspect the namespace-open/join path before patching.

- 2026-07-29 11:19 UTC — Correction implementation passes focused verification: `cargo test -p cix-run --lib` now runs 38 tests and warning-denied `cix-run` clippy passes. Shell and explicit-command lookup share one resolver, preserving the service PATH's order before `/usr/bin:/bin`; debug resolves a bare override before unit generation and exec resolves after entering the private mount namespace. The empty-environment regression uses an isolated fake operator path, and a namespace test proves the caller's own five handles classify as shared. Exec now compares namespace device/inode identities, calls `setns` only for differing handles, and prints both the private and caller-shared sets; D34 and the docker ledger state that PID is currently host-shared and network is shared for a port-declaring service. Next: run the live debug/exec corrections against nginx and capture literal outputs.

- 2026-07-29 11:26 UTC — Correction to the 11:02 overclaim, preserving the original entry: the successful original exec probes used absolute operator paths (`/bin/sh`/`/usr/bin/id`); summarizing them as bare `id` hid the resolver defect, and there is no retained transcript supporting the bare spelling. The nginx `ps` output was the host process view, not evidence of a private PID namespace. Fresh re-verification used `/nix/store/cx83…-nginx-cix`, whose live unit reported literal `Environment=`. `/proc` handle comparison for `cix-run-nginx-18c6becea6f7ef820.service` found `mnt` private and `pid`, `net`, `ipc`, and `uts` caller/host-shared; thus the correction prompt's parenthetical expectation that IPC/UTS were private was also broader than this observed unit. The new banner reported exactly `private namespaces [mount]` and `caller-shared namespaces [network, IPC, UTS, PID]`. `sudo cix exec nginx -- id` returned runtime `uid=64130 gid=64130`; exact-unit `--root -- id` returned `uid=0(root) gid=0(root)`. A bare `sh` exec showed nginx master/worker entries in the explicitly host-shared PID view and created `/var/cache/nginx/correction-exec-write` as `64130:64130`.

- 2026-07-29 11:26 UTC — Fresh debug gate transcripts: system one-shot mode resolved bare `sh`, ran as DynamicUser `61489:61489`, read `daemon off;` at projected `/etc/nginx/nginx.conf`, created `/var/cache/nginx/correction-debug-write` as that identity, and observed both the `/srv/www` item projection and an undeclared `/etc` destination reject writes. The real default interactive path printed `using shell /usr/bin/sh (operator fallback PATH)`, ran as DynamicUser `64716:64716`, repeated the config/cache/denial checks, and exited cleanly. `debug --user -- sh -c ...` printed the D13 banner plus the host's mount-namespace fallback diagnostics, then ran as caller `1001:1001` and read the item via `$CIX_APP`. Live and debug units stopped; the three named correction files were unlinked, a system-manager reload cleared four pre-existing stale not-found listener entries, and both managers plus transient-unit directories are clean. Next: rerun focused tests after the final regression assertion, then the complete workspace/tour/VM gate.

- 2026-07-29 11:29 UTC — Full correction gate passes on the finished tree: `cargo fmt --all --check`, `cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, and `nix build .#checks.x86_64-linux.vm-dogfood`. Workspace tests include deterministic tour generation and committed-document drift, 38 corrected cix-run unit tests, rootless systemd integration, and root-gated system projection/listener tests. The latter leave no transient files or processes but do leave eight `not-found/failed` manager cache entries; explicit failed-state cleanup plus a manager reload removed those cache-only entries. No code, docs, or live-gate failures remain. Next: final diff audit and commit.
