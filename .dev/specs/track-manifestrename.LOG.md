# track-manifestrename work log

- 2026-07-29T10:30:01Z — Started D33’s repo-wide baked-file/key rename on `track/manifestrename`. Read AGENTS.md, the repository journal, D33, and the tour harness. The harness’s documented regeneration command is `cargo test --test tour -- --ignored generate_tour`; generated tour pages will be updated only through that command. Next: perform mechanical scoped substitutions, rename the Cixfile golden fixture, then run the full required gate.

- 2026-07-29T10:31:25Z — Renamed every in-scope baked-file reference to `cix-manifest.json`, every schema key to `cixManifest`, and moved the Cixfile golden fixture with `git mv`. Preserved schema-language terminology as “spec” (for example, “spec v2”) instead of turning it into “manifest v2”. Ran `cargo fmt --all`, then regenerated the tour via `cargo test --test tour -- --ignored generate_tour` (1 passed). The generated Cixfile page changed as expected; the compose page’s fixture `narHash` also changed because its baked manifest content changed. The required legacy-spelling grep now reports only OUT-scoped historical logs/spec records. Next: run `cargo build`, the full test suite, then re-run the exact scan and review before committing.

- 2026-07-29T10:33:00Z — Final verification passed after also renaming Rust identifiers that directly represented the manifest file/key (`specFile`→`manifestFile`, `spec_summary`→`manifest_summary`, `cix_spec`→`cix_manifest`). `cargo build` passed; `cargo test` passed (including tour drift/determinism and all workspace tests); `cargo fmt --all --check` and `git diff --check` passed. The required exact grep reports only OUT-scoped `LOG.md` files and historical `.dev/specs/track-*.md` files. Ready to commit.
