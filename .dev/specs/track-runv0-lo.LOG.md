# track/runv0-lo work log

- 2026-07-29T16:48:23Z — Started the D39 best-effort-loopback correction on `track/runv0-lo`. Read `AGENTS.md`, the session journal, D39, the prior RUN track log, and the current sandbox/test implementation. The worktree was clean and the devenv shell was not activated, so verification will run via `nix develop`. `bwrap --unshare-net` owns loopback setup and exits before it can run the command when that netlink operation is AppArmor-denied; the implementation therefore needs to create the isolated network namespace separately, try loopback setup itself, and pass that namespace to bwrap without weakening the final sandbox. Next: implement that bootstrap and focused error-hint coverage.

- 2026-07-29T17:20:00Z — Explored a direct clone/bootstrap implementation and discarded it without retaining product changes: Linux/bubblewrap requires the user, mount, PID, IPC, UTS, network, and cgroup namespaces to be established in the same privileged setup sequence. A separately created user/network namespace either loses the capabilities bwrap needs for its remaining setup or makes `/proc/self/ns/cgroup` inaccessible; local real-RUN integration therefore correctly failed. The viable implementation must change/bundle bwrap's own `loopback_setup()` behavior (it currently calls `die_with_error` on netlink failures) or otherwise provide a bwrap-native status channel; retrying with host networking is explicitly not acceptable. Focused formatter coverage and compilation of the discarded experiment passed; its actual RUN test failed as expected, so no gate/commit claim is valid yet.

- 2026-07-29T17:09:22Z — Implemented the orchestrator's superseding two-tier redesign, without patching bubblewrap. A single silent probe per build chooses bwrap `--unshare-net` when usable; otherwise every RUN uses bwrap's unchanged user/PID/UTS/IPC/cgroup/mount sandbox plus a 13-instruction classic-BPF seccomp program. Exact program: validate the native audit architecture (kill on mismatch), load the syscall number, allow every syscall except `socket`/`socketpair`, load argument 0 for those two calls, return `EPERM` for `AF_INET` (2), `AF_INET6` (10), or `AF_PACKET` (17), and allow other families including `AF_UNIX`/`AF_NETLINK`. The filter is passed as compiled 8-byte `sock_filter` records through a dedicated inherited fd; FETCH and its resolver mounts are untouched. Added bytecode-semantic coverage, a forked child that installs the actual kernel filter and proves AF_INET→EPERM plus AF_UNIX success, and focused formatting coverage for the fallback-only localhost hint. Updated the Cixfile sandbox documentation with the invariant promise and both mechanisms. Next: compile and run focused tests, then force the fallback path through real bubblewrap before the full gate.

- 2026-07-29T17:11:41Z — Focused implementation gate passes: `cargo test -p cix-cixfile --lib` (28 tests, including a forced socket-filter bubblewrap execution), the existing real-Nix RUN miss/hit integration, and `cargo clippy -p cix-cixfile --all-targets -- -D warnings`. Corrected the filter from the initially logged 13 instructions to 14 after auditing syscall-level escape routes: before the default allow, it now returns EPERM from `io_uring_setup` too, because `IORING_OP_SOCKET` can otherwise create an internet socket without a `socket(2)` syscall. The exact final instruction flow is: load+validate audit arch; load syscall; branch `socket`/`socketpair` to family checks; branch `io_uring_setup` directly to EPERM; otherwise allow; for the two socket syscalls load arg0 and return EPERM on AF_INET/AF_INET6/AF_PACKET, otherwise allow. The direct kernel test covers INET denial and UNIX allowance; the forced-bwrap test covers fd inheritance and bwrap loading the exported byte stream. `devenv shell` was used because this worktree's direnv activation is blocked; it generated an unrelated untracked `devenv.lock`, which was removed. Next: full workspace fmt/build/clippy/tests and explicit tour checks, then the live projB and VM gates.

- 2026-07-29T17:17:07Z — Full first-pass gate passed: workspace fmt/build/warning-denied all-target clippy/tests; explicit tour regeneration, determinism, and committed drift (no page changes); local `target/debug/cix build examples/build/projB` with FETCH/RUN memo hits and final `/nix/store/ig5b88h0sbg7x24m559k8r9x7s832dbl-cixfile-item`; and `nix build .#checks.x86_64-linux.vm-dogfood --no-link -L`. The VM ran under TCG because KVM was unavailable and passed all live services; its known Node-as-PID-1 SIGTERM timeout took 90 seconds before systemd killed it, then cleanup passed. Final adversarial review found the x86_64 x32 ABI shares `AUDIT_ARCH_X86_64` while setting bit 30 on syscall numbers, which would bypass plain native-number equality checks. The final filter is therefore 15 instructions: after loading the syscall number it rejects values ≥ `0x40000000` with EPERM before the native socket/io_uring dispatch. This rejects x32 (and other non-native high-bit syscall encodings) rather than creating an ABI escape. Next: rerun focused semantics/bwrap, workspace short gate, and cached VM on this final filter; then audit and commit.

- 2026-07-29T17:21:44Z — Final staged-source gate passes after the x32 hardening. `cargo test -p cix-cixfile --lib` passes all 28 tests, including bytecode interpretation, actual kernel seccomp enforcement, fallback hint formatting, and forced filter loading/execution through real bubblewrap. The first direct-shell rerun exposed a test-fixture assumption (it selected Ubuntu `/usr/bin/bash`, not an offered Nix-store shell); the fixture now discovers a real `/nix/store/*/bin/bash`, and passes both inside devenv and from the host shell. `cargo clippy --workspace --all-targets -- -D warnings` and `cargo test --workspace` pass; the latter reruns tour determinism/drift and the real-Nix RUN integration. After staging the complete source for flake visibility, `nix build .#checks.x86_64-linux.vm-dogfood --no-link` rebuilt cix and passed the full VM gate again. Final audit: FETCH remains host-networked and unchanged; preferred RUN stays `--unshare-net`; fallback RUN retains every other bwrap namespace/mount constraint, adds the fixed 15-instruction filter through an inherited fd, prints nothing on success, and adds exactly one localhost hint line only on failure. Next: final fmt/status/diff audit, exact-source projB memo build, then commit on `track/runv0-lo`.
