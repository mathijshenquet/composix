# track/regen1 — assemble wave-1 regenerated corpus cases (luna, cold)

Read AGENTS.md first (gate convention; synchronous receipts), then
docs/corpus.md §"How this corpus is maintained (the loops)". Work in the
herdr worktree on branch `track/regen1`. Keep `corpus/migrate/LOG.md`
current (dated track heading).

Six cases were cold-regenerated by gpt-5.6-luna in staging dirs
(`/home/mathijs/regen-stage/{caddy,memcached,nats,nginx,redis,traefik}`)
against the post-CIP-91/92 canon, and every build was independently
re-verified green by the orchestrator. Your job: land them in
`corpus/migrate/` with honest ledgers and receipts. Calibration from
Mathijs: upstream-layout mirroring is a default against *unnecessary*
deviation, not a 100% target — do not churn the files for taste nits;
carry nits as prose where worth noting, silence where not.

## Per case (all six)

1. Copy from the staging dir into `corpus/migrate/<case>/`: `Cixfile`,
   `Cixfile.dissolved`, both `.lock` files, and aux files the Cixfiles
   reference (e.g. redis's `redis-start.sh`). Do NOT copy `bin/`,
   `TASK.md`, `MIGRATE.md`, `NOTES.md`, or `context/`. The old
   single-form Cixfile is replaced; nothing else in the case dir is
   deleted.
2. Rewrite `GAPS.md` fresh:
   `Generated: migrate.md@00078d9 · gpt-5.6-luna · 2026-08-04`,
   `Status: current`. Derive gap bullets from the staging `NOTES.md`
   plus the orchestrator findings below; route with arrows per the
   convention. Resolved old gaps disappear; still-true old gaps carry
   over.
3. Append a dated section to `receipt.md`: "2026-08-04 regeneration
   (cold, gpt-5.6-luna)" recording the exact build commands for both
   twins and your probe run (below), all as synchronous exit statuses.
4. Probe: fetch context where the case has one (`bash
   corpus/migrate/fetch.sh <case>`), then run
   `CIX=<worktree>/target/debug/cix ./check.sh cix` (build the worktree
   cix first: `cargo build -p cix`). Sequential, one case at a time
   (ports). If a probe cannot pass, record the exact failure honestly —
   do not fix by weakening the probe; a red probe is a finding.
   Docker-mode probes are out of scope (record "not rerun").

## Orchestrator findings to fold into the right GAPS.md

- **caddy**: XDG config folded into `/data` was FORCED by a verified
  product defect (CONFIGDIR must be under /etc at run time while build
  accepts it — see the docs/open-questions.md entry) → language, cite
  the open item. The `/etc/hosts` FILE exists because the service
  sandbox provides no `localhost` → language, cite the open item.
  Faithful twin otherwise resolves the old caddy gaps (real config
  contract, four ports incl. `udp:443`, faithful twin present).
- **traefik**: the two FETCH steps carry an identical, copy-pasted
  EXPECT that warm builds never validated — a verified product defect
  (open-questions entry) → case + language: leave the EXPECT values AS
  THEY ARE (the defect reproduction lives in this case; fixing the
  hash before the product fix would destroy the repro), state that
  plainly in the gap bullet. Also: pinning GitHub's release-metadata
  JSON is EXPECT-hostile (download counters mutate) → case, with the
  normalization fix deferred to the same product round.
- **nats**: upstream context files (nats-server.conf) unfetchable due
  to the SOURCE provenance gap; luna started with `--http_port`
  instead of the conf-file contract → evidence + case.
- **memcached**: dissolved twin carries upstream version ENVs that
  nixpkgs actually decides → prompt (the dissolved-contract addendum
  below fixes the class).
- **nginx, redis**: no orchestrator findings beyond taste nits; their
  old CIP-91-stale flags resolve.

## docs/migrate.md addenda (loop 2, write in the document's voice)

1. **The dissolved-twin contract**: when nixpkgs packages the app
   directly, the dissolved twin treats nixpkgs as authoritative — drop
   upstream version/checksum ENVs and Docker ceremony (entrypoint
   wrappers, metadata) unless behavior-relevant; keep the behavioral
   contract (ports, state, claims, probe-served behavior). The faithful
   twin is where upstream structure lives.
2. **Volatile fetched metadata**: fetching mutable API JSON (release
   metadata with download counters) makes the pin unstable — normalize
   inside the fetch to exactly the consumed fields, or fetch the
   stable asset URL directly.

## Ledger + close

- docs/corpus.md: re-grade the six rows to what the new receipts prove
  (Fidelity with the stated deviations; Evidence per your probe tier —
  the closed-root claim only where the CIP-84 audit roster actually
  covers the case and the gate run proves it).
- Regenerate the corpus browser; commit regenerated output.
- Do not touch other cases, cips/, or crates/ (fences).

## Gate

Standard agent tier (fmt, examples fmt, warning-denied clippy, full
workspace tests, tour regen+drift, corpus browser drift) plus the
focused closed-root audit scenario (your changes enter its roster).
Bounded. Synchronous receipts with exact repro commands in your LOG.
